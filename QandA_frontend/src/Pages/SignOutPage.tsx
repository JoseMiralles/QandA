import React from "react";
import { Page } from "../Page";

interface Props {
    action: string;
}

export const SignOutPage = (props: Props) => (
    <Page title="Sign Out">{ null }</Page>
);